import { WoundpenaltyPipe } from './woundpenalty.pipe';

describe('WoundpenaltyPipe', () => {
  it('create an instance', () => {
    const pipe = new WoundpenaltyPipe();
    expect(pipe).toBeTruthy();
  });
});
