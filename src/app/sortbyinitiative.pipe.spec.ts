import { SortbyinitiativePipe } from './sortbyinitiative.pipe';

describe('SortbyinitiativePipe', () => {
  it('create an instance', () => {
    const pipe = new SortbyinitiativePipe();
    expect(pipe).toBeTruthy();
  });
});
